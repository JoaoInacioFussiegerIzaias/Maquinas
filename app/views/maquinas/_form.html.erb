<%= form_with(model: maquina) do |form| %>
  <% if maquina.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(maquina.errors.count, "error") %> prohibited this maquina from being saved:</h2>

      <ul>
        <% maquina.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, class: "block text-gray-700" %>
    <%= form.text_field :nome %>
  </div>
<br>
  <div>
    <%= form.label :modelo, class: "block text-gray-700" %>
    <%= form.text_field :modelo %>
  </div>
<br>
  <div>
    <%= form.submit "Adicionar máquina", class: "bg-white border p-4 border-gray-500 text-black hover:bg-gray-200 px-4 py-1"%>
  </div>
<% end %>
